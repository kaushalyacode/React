/*Purpose: Layout components that wrap pages(with headers, sidebars, etc.)

Example:

    / layouts
        MainLayout.jsx
        AuthLayout.jsx
Use these for shared UI frames across multiple pages.*/